<div class="jumbotron">
    <h1>Hello @User.Identity.Name!</h1>
    <h2>Your claims:</h2>
    <ul>
        @foreach(var claim in User.Claims)
        {
            <li>
                @claim.Type: @claim.Value
            </li>
        }
    </ul>
</div>